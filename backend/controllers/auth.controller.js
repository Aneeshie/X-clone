
export const login= (req,res) => {
    return res.json({message: "you hit the login route"});
}
export const logout= (req,res) => {
    return res.json({message: "you hit the logout route"});
}
export const signIn = (req,res) => {
    return res.json({message: "you hit the signIn route"});
}
